[package]
name = "hll_to_ir"
version = "0.1.0"
edition = "2024"
rust-version = "1.92"

[lib]
name = "hll_to_ir"
path = "src/lib.rs"

[dependencies]
build_manifest = { path = "../build-manifest" }
os_runtime = { path = "../os-runtime" }
log      = "0.4.27"

[lints]
workspace = true
