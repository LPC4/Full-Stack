[package]
name = "virtual_machine"
version = "0.1.0"
edition = "2024"
rust-version = "1.92"

[lib]
name = "virtual_machine"
path = "src/lib.rs"

[dependencies]
firmware      = { path = "../firmware" }
asm_to_binary = { path = "../asm-to-binary" }

[lints]
workspace = true
