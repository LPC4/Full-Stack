; @version 2
external console_writeln: (str: u8*)

main: () -> i32 {
    console_writeln("hello from user mode!".ptr)
    while 1 == 1 {
        asm {
            li   a7, 2      ; yield
            ecall
        }
    }
    return 0
}
