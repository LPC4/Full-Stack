; Shared standard library types.

; Strings are the compiler's native { data: u8*, length: u64 } layout.
type Str = {
    data: u8*,
    length: u64,
}

; Heap allocator metadata for the linked free-list.
type HeapBlock = {
    next: HeapBlock*,
    ptr: u8*,
    size: u64,
    is_free: u64,
}

