; Test string literal compilation to u8[] slices
; String literals lower to a { ptr: u8*, len: u64 } slice fat pointer

main: () -> i32 {
    ; Declare variables with type annotations and assign string literals
    str1: u8[] = "Hello World"

    ; String with escape sequences
    str2: u8[] = "Line 1\nLine 2\tTabbed"

    ; Empty string
    str3: u8[] = ""

    return 0
}
