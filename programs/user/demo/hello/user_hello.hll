console := import("console")

main: () -> i32 {
    console.writeln("hello from user mode!".ptr)
    while 1 == 1 {
        asm {
            li   a7, 2      ; yield
            ecall
        }
    }
    return 0
}
